package com.github.marcelldechant.bistro;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BistroApplicationTests {

    @Test
    void contextLoads() {
        // This test is used to check if the Spring application context loads successfully.
        // If there are any issues with the application context, this test will fail.
        // No additional assertions are needed here, as the test will automatically fail
        // if the context cannot be loaded.
    }

}
